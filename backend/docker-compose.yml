services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: finanzapp
      POSTGRES_USER: finanz
      POSTGRES_PASSWORD: finanz123
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
